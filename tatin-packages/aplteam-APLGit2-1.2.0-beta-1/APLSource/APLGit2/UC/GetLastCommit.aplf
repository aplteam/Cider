 r←GetLastCommit(space folder args);full;branch
⍝ Desc: Returns the latest hash for the current (or the given) branch
⍝ Parse: 1s -full -branch=
⍝ Project: 1
 full←0 args.Switch'full'
 branch←''args.Switch'branch'
 r←branch ##.GetLastCommit folder
 :If ~full
     r←7↑r
 :EndIf
⍝Done
