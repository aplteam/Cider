 r←ListBranches(space folder args);parms
⍝ Desc: Lists all branches for a Git-managed project
⍝ Parse: 1s -a -r
⍝ Project: 1
 :If (,0)≢,args.a
     parms←'-a'
 :ElseIf (,0)≢,args.r
     parms←'-r'
 :Else
     parms←''
 :EndIf
 r←⍪parms ##.ListBranches folder
⍝Done
