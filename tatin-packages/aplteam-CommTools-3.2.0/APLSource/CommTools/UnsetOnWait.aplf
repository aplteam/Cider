 {r}←UnsetOnWait
⍝ Removes the function established by `SetOnWait`, if any.\\
⍝ Note that `Cleanup` does not do this.\\
⍝ Returns a shy result: 1 if a function was removed, and 0 if none was established.
 r←3=⎕NC'∆OnWait'
 ⎕EX'∆OnWait'
⍝Done
