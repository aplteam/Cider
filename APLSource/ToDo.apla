'Morten',(⎕UCS 13),'======',(⎕UCS 13 13),'* Search for the string "⍝Morten⍝"',(⎕UCS 13),'* Check the file "cider.config.template" - carries "⍝Morten⍝"',(⎕UCS 13 13 13),'Kai',(⎕UCS 13),'===',(⎕UCS 13 13),'* Add test case for `AddTatinDependencies`',(⎕UCS 13),'* The test case for `ListTatinPackages` needs to be amended',(⎕UCS 13),'* Add test case with "Tatin" missing as sub-key of "dependencies" and also make the code resilient',(⎕UCS 13),'  towards this: currently it crashes. Should re-introduce the sub-key',(⎕UCS 13),'* Currently when dependencies.tatin points to an empty folder Cider throws an error because it cannot',(⎕UCS 13),'  detect a file apl-dependencies.txt - that''s a bit harsh.',(⎕UCS 13),'  Should we instead print a warning?',(⎕UCS 13),'* The documentation does not reflect any of the changes since (including) version 0.34.0',(⎕UCS 13),'* The user command script needs attention once the NuGet stuff is available',(⎕UCS 13),'* Document ⎕SE.Cider.DEVELOPMENT somehow'
