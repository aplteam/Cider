 R←Test_NuGet_020(stopFlag batchFlag);⎕TRAP;tempFolder;parms;I;rc;msg;debug
⍝ Open a project and add some NuGet dependencies wrongly separated by a space
 ⎕TRAP←(999 'C' '∘∘∘ ⍝ Deliberate error')(0 'N')
 R←T._Failed

 ⎕TRAP←(999 'C' '∘∘∘ ⍝ Deliberate error')(0 'N')
 R←T._Failed

 tempFolder←∆GetFolderInTempDir⊃⎕SI
 debug←0
 parms←debug ∆NuGetTestProcessParms(⊃⎕SI)tempFolder
 I←⎕NEW APLProcess(,⊂parms)
 'Something went wrong!'Assert 0=I.ExitCode
 R←T._OK

∆TidyUp:
 (rc msg)←FilesAndDirs.RmDirByForce tempFolder
 msg Assert 0=rc
⍝Done
