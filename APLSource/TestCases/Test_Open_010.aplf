 R←Test_Open_010(stopFlag batchFlag);⎕TRAP;parms;folder;r;res;links
⍝ Open an existing project with the "import" flag set (=no Link is created)
 ⎕TRAP←(999 'C' '∘∘∘ ⍝ Deliberate error')(0 'N')
 R←T._Failed

 'TEMP'#.⎕NS''
 folder←∆GetTestPath⊃⎕SI
 links←⎕SE.Link.Links''

 parms←''
 parms,←⊂folder
 parms,←⊂''                         ⍝ Project space
 parms,←⊂'#.TEMP'                   ⍝ parent
 parms,←⊂''                         ⍝ alias (tested separately)
 parms,←⊂5                          ⍝ import flag + quiet flag
 →T.GoToTidyUp~##.Cider.OpenProject parms

 →T.GoToTidyUp'World'≢#.TEMP.Foo.Hello
 →T.GoToTidyUp links≢⎕SE.Link.Links''

 R←T._OK

∆TidyUp:
 #.⎕EX'TEMP'
⍝Done
