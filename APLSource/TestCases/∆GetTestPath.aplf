 r←∆GetTestPath add
 r←(FilesAndDirs.AddTrailingSep ##.CiderConfig.HOME),'Tests/',add
 r←{⍵/⍨~'//'⍷⍵}r   ⍝ Under some circumstances, "//" in a file path makes Dyalog disappear on Mac-OS, so we must make sure this does not happen
