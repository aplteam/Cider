 R←Test_Open_009(stopFlag batchFlag);⎕TRAP;parms;folder;r;rc
⍝ Open a simple project with a parent passed as a reference (converted to a name)
 ⎕TRAP←(999 'C' '∘∘∘ ⍝ Deliberate error')(0 'N')
 R←T._Failed

 folder←∆GetTestPath⊃⎕SI

 parms←⎕NS''
 parms.folder←folder
 parms.parent←⎕THIS
 parms.projectSpace←'TEMP'
 parms.quietFlag←1
 →T.GoToTidyUp~##.Cider.OpenProject parms

 →T.GoToTidyUp(≢⍕⎕THIS)≠≢parms.parent

 R←T._OK

∆TidyUp:
 {}##.Cider.CloseProject(⍕⎕THIS),'.TEMP'
 ⎕EX'TEMP'
⍝Done
