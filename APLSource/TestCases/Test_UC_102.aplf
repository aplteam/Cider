 R←Test_UC_102(stopFlag batchFlag);⎕TRAP;rc;msg;res;stop;path
⍝ List all installed dependencies in a particular installation folder with just the -raw flag
 ⎕TRAP←(999 'C' '∘∘∘ ⍝ Deliberate error')(0 'N')
 R←T._Failed

 path←##.CiderConfig.HOME,'/TestData/MyUCMDs'
 stop←0
 (rc msg res)←stop ∆UCMD'ListTatinDependencies "',path,'" -raw'
 Assert rc=0
 →T.PassesIf 0<≢res
 →T.PassesIf 2=⍴⍴res
 →T.PassesIf 5= 2⊃⍴res
 →T.PassesIf ∧/res[;3]∊0 1
⍝Done
