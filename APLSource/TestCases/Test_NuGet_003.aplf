 R←Test_NuGet_003(stopFlag batchFlag);⎕TRAP;folder;parms;res;log
⍝ Open a project that has both NuGet and Tatin dependencies
 ⎕TRAP←(999 'C' '∘∘∘ ⍝ Deliberate error')(0 'N')
 R←T._Failed

  →0 ,R←T._NotImplemented  ⍝TODO⍝  ⍝TODO⍝  ⍝TODO⍝  ⍝TODO⍝  ⍝TODO⍝  ⍝TODO⍝

 #.⎕SHADOW'TEMP' ⋄ 'TEMP'#.⎕NS''
 folder←∆GetTestPath⊃⎕SI
 ∘∘∘ ⍝Morten⍝  That folder must carry proper NuGet definitions

 parms←⎕NS''
 parms.folder←folder
 parms.noPkgLoad←0
 parms.watch←'ns'
 parms.ignoreUserExec←1
 parms.batch←1
 (res log)←##.Cider.OpenProject parms

 →T.GoToTidyUp~res
 ∘∘∘                                    ⍝ More tests

 R←T._OK

∆TidyUp:
 res←∆LINK'Break' '#.TEMP'
 Assert res ∆StartsWith'Unlinked:'
⍝Done
