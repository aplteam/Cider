 R←Test_NuGet_010(stopFlag batchFlag);⎕TRAP;tempFolder;parms;I;rc;msg;debug
⍝ Open a project that has already two NuGet dependencies
 ⎕TRAP←(999 'C' '∘∘∘ ⍝ Deliberate error')(0 'N')
 R←T._Failed

 tempFolder←∆GetFolderInTempDir⊃⎕SI
 debug←0
 parms←debug ∆NuGetTestProcessParms(⊃⎕SI)tempFolder
 I←⎕NEW APLProcess(,⊂parms)
 'Something went wrong!'Assert I.ExitCode=130
 R←T._OK

∆TidyUp:
 (rc msg)←FilesAndDirs.RmDirByForce tempFolder
 msg Assert 0=rc
⍝Done
