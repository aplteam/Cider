 r←Test_ListDeps_001(stopFlag batchFlag);⎕TRAP;buff
⍝ Exercise ListDependencies on a project that has just Tatin dependencies
 ⎕TRAP←(999 'C' '. ⍝ Deliberate error')(0 'N')

 r←T._Failed

 buff←##.Cider.ListDependencies'[cider]'
 →T.PassesIf 2=⍴⍴buff
 →T.PassesIf 0<≢buff

 r←T._OK
