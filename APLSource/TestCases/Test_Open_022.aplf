 R←Test_Open_022(stopFlag batchFlag);⎕TRAP;parms;r
⍝ Open a project and  "projectSpace" via parameter
 ⎕TRAP←(999 'C' '∘∘∘ ⍝ Deliberate error')(0 'N')
 R←T._Failed

 #.⎕SHADOW'TEMP' ⋄ 'TEMP'#.⎕NS''

 parms←##.Cider.CreateOpenParms ⍬
 parms.folder←∆GetTestPath⊃⎕SI
 parms.importFlag←1
 parms.quietFlag←1
 parms.projectSpace←'TEMP'                  ⍝ Overwrites the config file
 →T.GoToTidyUp~##.Cider.OpenProject parms
 →T.GoToTidyUp'World'≢#.TEMP.Hello          ⍝ Because parent is # according to the config file

 R←T._OK

∆TidyUp:
 #.⎕EX'TEMP'
⍝Done
