 R←Test_Tatin_005(stopFlag batchFlag);⎕TRAP;res;folder;parms;dmx
⍝ Attempt to load project with Tatin packages but with an absolute path (invalid)
 ⎕TRAP←(999 'C' '∘∘∘ ⍝ Deliberate error')(0 'N')
 R←T._Failed

 'TEMP'#.⎕NS''
 'FOO'#.⎕NS''
 folder←∆GetTestPath⊃⎕SI

 parms←''
 parms,←⊂folder
 parms,←⊂''
 parms,←⊂''
 parms,←⊂''
 parms,←⊂1

 :Trap 0
     res←##.Cider.OpenProject parms
     →T.GoToTidyUp 1
 :Else
     dmx←⎕DMX
     →T.GoToTidyUp~dmx.EM ∆StartsWith'Target namespace for Tatin packages is addressed with absolute path (invalid)'
 :EndTrap

 R←T._OK

∆TidyUp:
 res←∆LINK'Break' '#.TEMP'
 Assert res ∆StartsWith'Unlinked:'
 #.⎕EX'TEMP'
 #.⎕EX'FOO'
⍝Done
