 R←Test_Open_006(stopFlag batchFlag);⎕TRAP;parms;folder;r;res
⍝ Load project but suppress execution of the init function with positional parms
 ⎕TRAP←(999 'C' '∘∘∘ ⍝ Deliberate error')(0 'N')
 R←T._Failed

 'TEMP'#.⎕NS''
 folder←∆GetTestPath⊃⎕SI

 parms←''
 parms,←⊂folder
 parms,←⊂''                         ⍝ Project space
 parms,←⊂'#.TEMP'                   ⍝ parent
 parms,←⊂''                         ⍝ alias (tested separately)
 parms,←⊂3                          ⍝ quietFlag + read-only
 →T.GoToTidyUp~⎕SE.Cider.OpenProject parms

 →T.GoToTidyUp'World'≢#.TEMP.Foo.Hello
 →T.GoToTidyUp 0≠#.TEMP.Foo.⎕NC'∆GLOBAL_TestOpen_006'

 R←T._OK

∆TidyUp:
 res←∆LINK'Break' '#.TEMP.Foo'
 Assert res ∆StartsWith'Unlinked:'
 #.⎕EX'TEMP'
⍝Done
