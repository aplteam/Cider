 r←Test_Misc_010(stopFlag batchFlag);⎕TRAP;buff;filename;res;ns;expected
⍝ Exercise ProjectConfig
 ⎕TRAP←(999 'C' '. ⍝ Deliberate error')(0 'N')
 :If batchFlag
     r←T._NoBatchTest
     :Return
 :EndIf
 r←T._Failed

 CommTools.Pause'This test needs you! Simply close the editor window after pressing <enter>. Press <enter> to continue.'

 filename←##.CiderConfig.HOME,'/cider.config'
 buff←⊃⎕NGET filename
 res←##.Cider.ProjectConfig ##.CiderConfig.HOME
 ns←⎕JSON⍠('Dialect' 'JSON5')⊢res
 (⊂buff)⎕NPUT filename 1 ⍝ Paranoia
 expected←'CIDER' 'LINK' 'SYSVARS' 'USER'
 →T.PassesIf expected≡' '~¨⍨↓ns.⎕NL 9

 r←T._OK
⍝Done
