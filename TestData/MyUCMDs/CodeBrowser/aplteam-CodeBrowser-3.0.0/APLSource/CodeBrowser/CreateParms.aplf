 r←CreateParms path
⍝ If `path` is not empty it is assumed that this points to the CodeBrowser home directory of a user command folder.
⍝ | Name             | Description | Options/flag |
⍝ |:-----------------|:------------|:-------------|
⍝ | `backgroundColorPrint` | Set this to 0 if you don't want background color when printing. | --- |
⍝ | `caption`        | Defaults to the list of namespaces specified by the user. | `-caption |
⍝ | `cssfilename`    | Location of the CSS file. | --- |
⍝ | `filename`       | If not set this defaults to a temp file name. | `-filename` |
⍝ | `footer`         | No default. Text string added to the bottom of the HTML file. | `-footer` |
⍝ | `ignore`         | Use this to ignore, say, a help system etc.<<br>>Text vector with comma-separated fully qualified namespace names. | `-ignore` |
⍝ | `ignoreEmpty`    | Boolean that defaults to 1 menaing that empty containers are ignored. | --- |
⍝ | `ignoreTatinPkgs`| Boolean that decided whether #._tatin (or ⎕SE._tatin) should be ignored |
⍝ | `info`           | If not empty this is shown under the caption with ID="info". | `-info` |
⍝ | `infoIsHTML`     | Boolean that defaults to 0, meaning that any `<` becomes `&lt;`.<<br>>Typically used for HTML that was generated by the `MarkAPL` class. |
⍝ | `lang`           | Specifies the language; defaults to "en". | `-lang` |
⍝ | `lines`          | Number of lines to be displayed. Defaults to no restriction |
⍝ | `linkcssfile`    | Boolean that defaults to 0 menaing that contents of `cssfile` is embedded. | --- |
⍝ | `namespaces`     | Will be becomes the namespaces to be scanned. Is read-only.| --- |
⍝ | `printFontSize`  | Base font size in pt. | `-pfs` |
⍝ | `processFunctions`   | Boolean that defaults to 1. | `-obj=f` |
⍝ | `processGuiInstances | Boolean that defaults to 0. | `-obj=g` |
⍝ | `processOperators`   | Boolean that defaults to 1. | `-obj=o` |
⍝ | `processScripts` | Boolean that defaults to 1. Regards classes, interfaces and scripted namespaces. | `-obj=s` |
⍝ | `processVars`    | Boolean that defaults to 1. | `-obj=v` |
⍝ | `recursive`      | Boolean that defaults to 1, meaning that sub-namespaces will be scanned as well. | `-r=1` |
⍝ | `showParms`      | Boolean that defaults to 0. Specify 1 to append a table with parameters. | `-p` |
⍝ | `twoSidedPrint`  | Boolean that defaults to 0. If 1 the left margin is always larger than the right one.| `-twosided` |
⍝ | `version`        | Text vector with version number and version data. Is read-only| --- |
⍝ | `viewInBrowser`  | Boolean that defaults to 0. Specify 1 for displaying the HTML in your default browser. | `-view` |
 r←⎕NS''
 r.backgroundColorPrint←1
 r.caption←¯1
 r.cssfilename←GetCssFilename path
 r.filename←''
 r.footer←''
 r.ignore←''
 r.ignoreEmpty←1
 r.ignoreTatinPkgs←1
 r.info←''
 r.infoIsHTML←0
 r.lang←'en'
 r.lines←¯1
 r.linkcssfile←0
 r.namespaces←''
 r.printFontSize←8
 r.processFunctions←1
 r.processGuiInstances←0
 r.processOperators←1
 r.processScripts←1
 r.processVars←1
 r.recursive←1
 r.showParms←0
 r.twoSidedPrint←0
 r.version←{(1⊃⍵),' ',(2⊃⍵),' from ',(3⊃⍵)}Version
 r.viewInBrowser←0
 r.⎕FX'r←∆List' 'r←{⍵,[1.5]⍎¨⍵}↓⎕nl 2' 'r←(~(r[;1]~¨'' '')∊''refs'' ''bookmarkCounter'')⌿r'
 ⍝Done
